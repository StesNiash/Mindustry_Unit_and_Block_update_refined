Events.on(EventType.PayloadDropEvent.class, event ->
{
    if (event.unit == null) return;
    var unit = event.carrier;
    var payload = event.unit;
    Log.info("");
    Log.info("Before remove");
    Groups.unit.each(Log::info);
    Log.info("");
    //Core.app.post(() -> {
    //    var unit_ = Groups.unit.getByID(pickup_id.get(payload));
    //    pickup_id.remove(payload);
    //    Log.info("After remove");
    //    Groups.unit.each(Log::info);
    //    unit_.add();
    //    Log.info("");
    //});
    Call.unitDestroy(pickup_id.get(payload));
    pickup_id.remove(payload);
    payload.physref.body.mass = payload.mass();
    payload.elevation = 0;
});


Call.unitDestroy(pickup_id.get(payload));
pickup_id.remove(payload);


Core.app.post(() -> {
                Log.info(Vars.net.client());
                if (Vars.net.client())
                {
                    Vars.netClient.clearRemovedEntity(pickup_id.get(payload));
                }
            });


Groups.unit.each(unit => {
if(unit.type.payloadCapacity > 8)
if(unit.payloads.size > 0){
unit.payloads.each(p => {

if(Boolean(p.class == UnitPayload)){


p.unit.elevation = 0.5;
p.unit.physref.body.mass = 0;
p.unit.add();

}}
)}
})